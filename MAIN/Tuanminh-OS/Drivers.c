#include <stdio.h>
#include <stdlib.h>

int main () {
    printf("loading drivers");
    FILE *in_file  = fopen("drv.sys", "r");
    FILE *in_file  = fopen("drvsound.c");
    return 0
}
int main2 () {
    printf("Going to safe mode")
    system("safemode.exe")
    return 0
}
